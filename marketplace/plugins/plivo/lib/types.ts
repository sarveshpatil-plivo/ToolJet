export type SourceOptions = {
  authId: string;
  authToken: string;
};

export type QueryOptions = {
  operation: string;
  to: string;
  from: string;
  body: string;
  answer_url: string;
  answer_method: string;
};
